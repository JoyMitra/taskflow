# Copy to .env.test if you want to override the test DB connection.
# Not required by default — tests/jest.setup.cjs falls back to this
# exact value if TEST_DATABASE_URL isn't set in your shell.

TEST_DATABASE_URL="postgresql://taskflow:taskflowtest@localhost:5433/taskflow_test?schema=public"
