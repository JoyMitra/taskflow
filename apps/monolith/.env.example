# Copy this file to .env for local development.
# Coolify will inject the real DATABASE_URL and PORT as environment
# variables on the deployed resource — you do NOT commit a real .env.

DATABASE_URL="postgresql://taskflow:taskflow@localhost:5432/taskflow?schema=public"
PORT=3000
